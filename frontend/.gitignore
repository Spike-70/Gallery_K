# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 로컬 환경 변수
# `.env`·`.env.development`·`.env.production`은 비밀값이 없어 저장소에 포함한다.
# `VITE_` 변수는 전부 번들에 실리므로 비밀값을 둘 자리가 애초에 없다.
.env.local
.env.*.local
