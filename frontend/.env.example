# 프런트엔드 환경 변수 — 프런트엔드 아키텍처 문서 §15.1
# 동일 오리진 배포이므로 상대 경로를 쓴다(API 문서 §2.11).
VITE_API_BASE_URL=/api
VITE_APP_ENV=dev

# 데모 스위치 — 백엔드 연동 시 false로 바꾼 뒤 src/mocks/README.md의 3단계를 따른다.
VITE_USE_MOCK=true

# 웹푸시 VAPID 공개키. 비어 있으면 알림 구독이 합성 값으로 대체된다.
VITE_VAPID_PUBLIC_KEY=
