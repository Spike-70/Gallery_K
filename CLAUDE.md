# 프로젝트 지침

## 1. 프로젝트 개요

- Backend는 AWS Chalice 기반이며 `backend/` 아래에 있다.
- Backend 가상환경은 `backend/.venv`에 있다. Backend 명령은 항상 해당 가상환경을 활성화한 상태에서 실행한다.
- Backend DB 마이그레이션은 Alembic으로 관리한다.
- Frontend는 React, Vite, TypeScript 기반이며 `frontend/` 아래에 있다.

## 2. 공통 작업 절차

작업 중에는 현재 어떤 절차의 어떤 단계에 있는지 각 단계 진입 직전에 짧게 보고한다.

### 2.1 표준 작업 절차

1. 시작: 사용자 요청의 핵심 의도를 요약하여 보고한다.
2. 연관 파일 스캔: 즉시 실행하지 말고 먼저 작업과 관련된 파일과 종속성을 스캔한다.
3. 추가 개선점 식별: 스캔한 파일에서 유지보수에 불리한 비표준 구현 패턴이나 기획과 충돌되는 지점을 찾는다.
4. 개선점 분류: 발견된 개선점은 `선행 작업 필요`, `마이너 픽스`, `백로그`로 구분한다. 개선점이 없으면 없다고 보고한다.
5. 작업 계획 제시: 선행 작업이 필요한 지점과 사용자 요청 작업에 대해 구조적이고 유지보수하기 쉬운 계획을 보고한다.
6. 실행: 보고한 계획에 따라 단계별로 처리한다.
7. 마무리: 마이너 픽스가 남아 있으면 처리하고, 결과와 백로그를 보고한다.

### 2.2 오류 수정 절차

1. 원인 분석: 즉시 수정하지 말고 관련 모듈을 추적하여 근본 원인을 파악한다.
2. 분석 보고: 원인 분석 결과를 먼저 보고한다.
3. 작업 계획 제시 및 실행: 계획을 제시한 뒤 단계별로 처리한다.
4. 유사 패턴 검색 및 일괄 개선: 다른 파일에도 같은 패턴의 오류가 있는지 확인하고 함께 개선한다.
